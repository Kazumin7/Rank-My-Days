Run this locally. Just use `npm run dev` in the project folder and it will start the frontend and backend and load your locally saved data automatically. Visit at [localhost port 3000.](http://localhost:3000/)